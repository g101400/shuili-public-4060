/* =====================================================================
 * ai_config.js — 智能分析引擎配置文件（v2.5.0，2026-09-15）
 * 作者：水利工程一张图开发组
 * 说明：模型密钥等部署相关配置集中于此，便于按环境注入，
 *       亦避免密钥随主引擎源码包外流。对外分发时请将 openrouterKey 留空，
 *       由使用者在「智能分析设置」中自行填写；如曾写入第三方密钥，
 *       建议到对应平台重置。
 * 加载顺序：必须在 js/ai.js 之前引入。
 * ===================================================================== */
window.SHUILI_AI_CONFIG = window.SHUILI_AI_CONFIG || {};
window.SHUILI_AI_CONFIG.openrouterKey = "";   // 已按软著要求置空，本地测试请在「智能分析设置」中填写自有密钥
